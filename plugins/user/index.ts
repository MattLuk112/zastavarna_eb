import api from './api';
import admin from './admin';
import server from './server';
import frontend from './frontend';

export const user = {
  api,
  admin,
  server,
  frontend,
};
export default user;
